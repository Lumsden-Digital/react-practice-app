import React from "react";

const Brand = () => {
  const style = {
    fontFamily: "Monoton",
    fontWeight: "400",
    fontSize: "5rem",
    margin: "0",
    padding: "0 1rem",
    maxWidth: "150px"
  };

  return <h1 style={style}>RL</h1>;
};

export default Brand;
